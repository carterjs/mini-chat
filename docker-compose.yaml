services:
  redis:
    image: redis
  app:
    build: ./app
    depends_on:
      - "redis"
    command: run --allow-net --allow-read --watch --unstable index.ts
    ports: 
      - "8000:8000"
    volumes: 
      - ./app:/app